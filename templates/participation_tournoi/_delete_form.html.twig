<form method="post" action="{{ path('app_participation_tournoi_delete', {'id': participation_tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation_tournoi.id) }}">
    <button class="btn">Delete</button>
</form>
