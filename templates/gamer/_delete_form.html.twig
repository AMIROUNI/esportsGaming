<form method="post" action="{{ path('app_gamer_delete', {'id': gamer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamer.id) }}">
    <button class="btn">Delete</button>
</form>
