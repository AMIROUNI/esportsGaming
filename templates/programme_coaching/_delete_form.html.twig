<form method="post" action="{{ path('app_programme_coaching_delete', {'id': programme_coaching.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_coaching.id) }}">
    <button class="btn">Delete</button>
</form>
